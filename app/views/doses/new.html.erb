<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description %>
<%= f.input :ingredient_id, collection: Ingredient.all%>
<%= f.submit value: "summit"%>
<%= link_to "Back", cocktails_path, class: "btn btn-primary"%>
<% end %>

